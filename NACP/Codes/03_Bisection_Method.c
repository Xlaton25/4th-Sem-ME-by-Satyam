#include<stdio.h>

int main(){
    // Bisection Method
    // 1. Find the mid point of the interval
    // 2. Check if the mid point is the root
    // 3. If not, check if the root is in the left or right half
    // 4. Repeat the process until the root is found
    
    return 0;
}